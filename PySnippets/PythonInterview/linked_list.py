"""
We need to implement a linked list that supports the following interface:
    1. Method append(val) that adds an element to the list in O(1) time.
    2. Method to get nth element of the list (we should also support negative indexing).
    3. We should able to use this list in `for` clause.
"""

